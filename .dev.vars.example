# نمونه‌ای از .dev.vars — کپی کنید: cp .dev.vars.example .dev.vars
# 🔑 رمز ورود پنل متغیر محیطی ندارد — پیش‌فرض: botpanel123 (تغییر از: تنظیمات ← امنیت و رمز عبور)
WEBHOOK_SECRET=dev-webhook-secret
BOT_TOKEN=
